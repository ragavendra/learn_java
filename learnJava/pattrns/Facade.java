
// Simple class to host and program many class/ objects
public class Facade {
	public void runGame(){
	/* 
	   GameConsole gc = new GameConsole();
	   GameObjects go = new GameObjects();
	   GameInputs gi = new GameInputs();
	   while(true){
	   		gi.getInput();

			go.update(gi);

			gc.draw(go);
	   }
	*/	
	}
}

// Adapter pattern via composition and inheritance c extends d implements g
